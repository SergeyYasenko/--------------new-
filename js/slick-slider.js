// SLICK CLIDER

$(document).ready(function () {
   $('.main__slider').slick({
      dots: true,
      adaptiveHeight: true,
      speed: 400,
      autoplay: false,
      autoplaySpeed: 5000,
      waitForAnimate: false,
      touchThreshold: 12,
   });
});